import { defineConfig } from 'vite';

// https://vitejs.dev/config/
export default defineConfig({
  // The 'base' property tells Vite to use the repository name as the base URL.
  // Replace 'genea-net' with your actual repository name.
  base: '/genea-net/',
  plugins: [],
});
